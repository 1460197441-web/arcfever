<view class="page-shell">
  <view class="nav-row">
    <view class="back-btn" bindtap="goBack">‹</view>
  </view>

  <view class="hero-panel">
    <view class="hero-tag">院校维护</view>
    <view class="hero-title">管理认证学校与学院列表</view>
    <view class="hero-copy">这部分是平台审核链路的基础设施。管理员新增学校和学院后，用户认证页会实时联动，避免填写机构信息时口径不一致。</view>
  </view>

  <view class="market-strip">
    <view class="market-note">
      <text class="market-emphasis">录入建议：</text>
      学院之间支持用换行、顿号、中文逗号或英文逗号分隔，提交后会自动拆分成可选项。
    </view>
  </view>

  <view class="section">
    <view class="card panel school-form-panel">
      <view class="section-title">新增认证学校 / 学院</view>
      <view class="helper-note">认证页会直接读取这里的数据，建议按学校正式名称录入。</view>

      <view class="form-label top-gap">学校名称</view>
      <input class="form-input" data-field="schoolName" value="{{schoolName}}" bindinput="onInput" placeholder="请输入学校名称" />

      <view class="form-label top-gap">学院列表</view>
      <textarea
        class="form-textarea"
        data-field="collegesText"
        value="{{collegesText}}"
        bindinput="onInput"
        placeholder="多个学院请用换行、顿号或逗号分隔"
      ></textarea>

      <view class="primary-btn full-btn top-gap" bindtap="submit">添加到认证列表</view>
    </view>
  </view>

  <view wx:if="{{loading}}" class="loading-state">正在同步学校与学院列表...</view>

  <view wx:elif="{{!list.length}}" class="section">
    <view class="card panel empty-panel">
      <view class="empty-title">还没有认证学校</view>
      <view class="empty-copy">先录入学校和学院，认证用户提交资料时才能做机构归属校验。</view>
    </view>
  </view>

  <view wx:else class="section list-stack">
    <view class="card panel school-card" wx:for="{{list}}" wx:key="id">
      <view class="feature-title">{{item.name}}</view>
      <view class="chip-row top-gap">
        <view class="chip" wx:for="{{item.colleges}}" wx:key="*this">{{item}}</view>
      </view>
    </view>
  </view>
</view>
