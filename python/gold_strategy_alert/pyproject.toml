[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "gold-strategy-alert"
version = "0.1.0"
description = "ICBC personal accumulation gold assist system"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
